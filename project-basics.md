this is an esphome project to track a baseball game based around working alongside a prebuilt project that tracks something else 


run this project by entering the dev environemnt with `nix develop` and then running `esphome run firmware/local-tracker-vars.yaml --device /dev/ttyACM0` to upload to the device and start logging



